import QtQuick 6.2
import QtQuick.Controls 6.2

import CustomComponents

BaseButton {
    id: root

    textColor: Style.accentColor
    textSize: 25
    buttonColor: Style.buttonSecondaryColor
    buttonPressedColor: Style.buttonPrimaryColor
}
